# 0.1.1-preview.1 release evidence

## Local candidate validation

On 2026-07-17, the exact locally packaged unsigned Preview candidate passed the locked Release
gate and Windows distribution checks:

- locked restore, formatting, dependency vulnerability inspection, Release build, and 111/111
  automated tests passed with zero warnings or errors;
- the MSI upgrade-sequence guard passed with `InstallInitialize=1500`,
  `RemoveExistingProducts=1501`, `InstallFiles=4000`, and `InstallFinalize=6600`;
- WinGet manifest validation passed;
- Chocolatey `pack` produced a valid NUPKG;
- the MSI was explicitly `NotSigned`, as required for this disclosed unsigned Preview path;
- MSI ProductVersion `0.1.101` upgrades Preview.2 (`0.1.2`) and CI.68 (`0.1.68`).

Local candidate hashes were:

| Artifact | SHA-256 |
| --- | --- |
| MSI | `08CFB2AD90BF41E618A8D7046D8C2021FCB29605791CF24A57CBC0844FCD0014` |
| Portable ZIP | `4C467CF5A0EC079A03858EED801675A06BB3D23925FE02C355131DCFA79F130F` |
| Chocolatey NUPKG | `C31B2DC056F42D4E126291305041B8AFD53FD6BE7CD7B1467C4C0C5A2FF04CA5` |
| WinGet manifest archive | `7330F2E5C34CD3F19F649434FA24319374311242A2D5464C191840CDB63B7492` |

These hashes identify the locally tested candidate. The protected tag workflow rebuilds the
release; its independently produced hashes must be recorded below and anonymously revalidated
before the go/no-go record is final.

## Clean Windows 11 installer lifecycle

The MSI above was exercised on an isolated Windows 11 Enterprise Evaluation 25H2 x64 VM
(`10.0.26200.0`) from 2026-07-17T20:18:49Z through 20:20:10Z. All 27 gates passed:

- clean-start and administrator context;
- checksum, stable UpgradeCode, distinct ProductCode, and ordered version checks;
- installation of immutable Preview.2;
- a deterministic post-`InstallFiles` failure returning `1603` and restoring the exact prior
  registration, executable, runtime configuration, Start-menu shortcut, and retained state;
- successful upgrade to MSI ProductVersion `0.1.101`;
- forced repair restoring a deliberately changed packaged file;
- downgrade rejection while preserving the current installation;
- uninstall registration/file/shortcut cleanup while retaining user state.

After the scenario, the VM had zero Vault Prospector registrations and processes. The test root was
removed, and the pre-existing encrypted local state remained byte-identical:

- database: `E9F47C102C0B2023A0B74F5B6C32B09C010DD71F203467F7AFEADE9C0C34B0D3`;
- second protected file: `61AB767AB04345A2F7A6065BAB58E41F5F8D22DAB27B9D31516803D461D8F7C9`.

The second protected file is the current-user DPAPI envelope for the metadata database material;
the recorded value is only its one-way SHA-256 digest, never the protected contents.

## Tagged release validation

Pending the exact source commit, protected CI and release workflow runs, immutable public asset
hashes, Sigstore/SBOM/provenance checks, and anonymous download verification.
