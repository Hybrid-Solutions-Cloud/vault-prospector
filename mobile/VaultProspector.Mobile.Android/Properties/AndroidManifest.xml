<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="internalOnly">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.USE_BIOMETRIC" />
  <application
      android:allowBackup="false"
      android:dataExtractionRules="@xml/data_extraction_rules"
      android:fullBackupContent="@xml/backup_rules"
      android:hasFragileUserData="false"
      android:icon="@drawable/vault_prospector"
      android:killAfterRestore="true"
      android:label="Vault Prospector"
      android:usesCleartextTraffic="false">
    <activity
        android:name="microsoft.identity.client.BrowserTabActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:host="auth"
            android:scheme="msal221af888-1c16-4637-9d45-b6dd2e1e7634" />
      </intent-filter>
    </activity>
    <!--
      Compile-time feasibility prototype only. It is intentionally disabled in production
      packages until exact encrypted mappings, fresh verification, physical-device origin
      negatives, and independent review are complete.
    -->
    <service
        android:name="cloud.hybridsolutions.vaultprospector.autofill.VaultProspectorAutofillService"
        android:enabled="false"
        android:exported="true"
        android:label="Vault Prospector AutoFill (prototype)"
        android:permission="android.permission.BIND_AUTOFILL_SERVICE">
      <intent-filter>
        <action android:name="android.service.autofill.AutofillService" />
      </intent-filter>
      <meta-data
          android:name="android.autofill"
          android:resource="@xml/autofill_service" />
    </service>
  </application>
</manifest>
